import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  button: {
    justifyContent: 'center',
    alignItems: 'center',
    margin: 8,
    borderWidth: 1
  },
  text: {
    lineHeight: 21,
    letterSpacing: 0.25
  }
})
